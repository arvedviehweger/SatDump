# iOS build output
build/
build-ios/
DerivedData/
*.xcodeproj/
*.xcworkspace/

# Prebuilt cross-compiled dependencies (large binaries, built separately)
deps/lib/
deps/include/
deps/share/

# Obsolete: the iOS port uses an in-tree Metal renderer, no fetch needed
imgui_backends/fetch_imgui_backends.sh

# macOS clutter
.DS_Store
